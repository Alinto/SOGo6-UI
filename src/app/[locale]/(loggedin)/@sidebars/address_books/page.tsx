import Sidebar from '@/features/address_books/components/sidebar/sidebar'
import React from 'react'

const AddressBooksSidebar: React.FC = () => {
  return <Sidebar key={'address_books_sidebar'} />
}

export default AddressBooksSidebar
