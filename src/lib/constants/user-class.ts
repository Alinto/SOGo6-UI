/** Wire values for the `user_class` field on /share endpoints (mail folders, calendars, address books). */
export const USER_CLASS_USER = 'user' // Simple user
export const USER_CLASS_GROUP = 'group' // Group of one or several users
export const USER_CLASS_RES = 'ressource' // Resource, location, room, things...
export const USER_CLASS_ANY = 'anyone' // Anyone (and anything) that can be authenticated
export const USER_CLASS_ANON = 'anonymous'
